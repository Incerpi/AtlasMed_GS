@model AtlasMed_GS.Models.Prontuario

@{
    ViewData["Title"] = "Excluir Prontuário";
}

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AtlasMed - Sua plataforma de gerenciamento de registros médicos</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #3498db;
            color: #333;
        }

        header {
            color: #fff;
            text-align: center;
        }

        section {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }

        div {
            background-color: #fff;
        }

    </style>
    <script>
        function confirmarExclusao() {
            var confirmacao = confirm("Tem certeza que deseja excluir este prontuário? Esta ação é irreversível!");

            if (confirmacao) {
                alert("Prontuário excluído com sucesso!");
                return true;
            } else {
                return false;
            }
        }
    </script>
</head>
<body>

    <h2>Excluir</h2>

    <div>
        <hr />
        <dl class="row">
            <dt class = "col-sm-2">
                Descrição:
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Descricao)
            </dd>
            <dt class = "col-sm-2">
                Diagnóstico:
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Diagnostico)
            </dd>
            <dt class = "col-sm-2">
                Alergias:
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Alergias)
            </dd>
            <dt class = "col-sm-2">
                Medicação:
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Medicacao.Nome)
            </dd>
        </dl>
    
        <form asp-action="Delete" onsubmit="return confirmarExclusao()">
            <input type="hidden" asp-for="IdProntuario" />
            <input type="submit" value="Excluir" class="btn btn-danger" /> |
            <a asp-action="Index">Voltar</a>
        </form>
    </div>

</body>
</html>
